import React from 'react';
import { shallow } from 'enzyme';
import SubmitTripForm from './SubmitTripForm';

describe('SubmitTripForm', () => {

  it('should pass a dummy test', () => {
    expect(true).toEqual(true);
  });

});